import { test } from 'vitest';
// Booking mutation coverage belongs to the atomic-RPC task; foundation constraints are tested separately.
test.skip('booking mutations await atomic-RPC task', () => {});
