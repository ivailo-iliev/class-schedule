# Supabase (public/client)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase (function-only — never expose or pass to a Vite build)
SUPABASE_URL=
SUPABASE_SECRET_API_KEY=

# Local database tests only; never configure this for a Netlify build
SUPABASE_DB_URL=

# Application
APP_ORIGIN=
