# Supabase (public/client)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server/function only — never expose)
SUPABASE_URL=
SUPABASE_SECRET_API_KEY=
SUPABASE_DB_URL=

# Application
APP_ORIGIN=

# ES256 signing key (function only — never expose, never commit)
SIGNING_JWK=
SIGNING_KID=imported-key-1
